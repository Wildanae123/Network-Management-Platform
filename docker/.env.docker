# Docker-specific environment
COMPOSE_PROJECT_NAME=network-management
POSTGRES_DB=network_management
POSTGRES_USER=netadmin
POSTGRES_PASSWORD=dockerpassword

# Service URLs (Docker internal)
DATABASE_URL=postgresql://netadmin:dockerpassword@postgres:5432/network_management
REDIS_URL=redis://redis:6379/0

# Port mappings
API_PORT=5000
FRONTEND_PORT=3000
POSTGRES_PORT=5432
REDIS_PORT=6379